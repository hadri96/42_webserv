#!/bin/bash

# A CGI script is a program or script that:

# 1. Accepts input (such as query strings or POST data) from the web server via environment variables or standard input (stdin).
# 2. Processes the input and performs some action (e.g., querying a database, computing results).
# 3. Outputs a valid HTTP response, starting with headers like Content-Type.
